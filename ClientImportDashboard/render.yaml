services:
  - type: web
    name: client-import-dashboard-api
    runtime: docker
    dockerfilePath: ./webApi/Dockerfile
    plan: free
    autoDeploy: true
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      - key: CORS_ALLOWED_ORIGINS
        sync: false
